---
interface Props {
  level: 1 | 2 | 3;
  class?: string;
  id?: string;
}
const { level, class: className, id } = Astro.props;
const Tag = `h${level}` as 'h1' | 'h2' | 'h3';
---
<Tag id={id} class={className}><slot /></Tag>
